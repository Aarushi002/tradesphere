# Used when building for production (npm run build / Vercel deploy)
# Frontend will call this backend; Kite redirect URL in setup = this URL + /api/kite/callback
REACT_APP_API_URL=https://tradesphere-fal1.onrender.com
