# TradingView webhook → order manager (paper + live)
# Use flexible versions so pip can pick builds with wheels for your Python (e.g. 3.14)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
websockets>=14.0
httpx>=0.27.0
python-dotenv>=1.0.0
aiosqlite>=0.20.0
